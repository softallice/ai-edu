package com.aiedu.backend.pm;

/** 활동(공수) 유형. koerp TimesheetActivityType(테이블) 를 v1 에서는 enum 으로 단순화 이관. */
public enum ActivityType {
    DEVELOPMENT, DESIGN, MEETING, DOCUMENTATION, SUPPORT, EDUCATION, ETC
}
